import React from "react";
import SightseeingListing from "../components/molecules/sightseeing/SightseeingListing";

/**
 * Sightseeing listing / search results.
 * Data is dummy until backend search API is ready — same route & navigation pattern as hotels.
 */
const SightseeingSearchPage: React.FC = () => {
  return <SightseeingListing />;
};

export default SightseeingSearchPage;
